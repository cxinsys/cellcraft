rule dataTable:
    input:
        "user/{username}/data/{filename}.csv"
    output:
        "workflow/data/DataTable_{username}_{filename}.csv",
        "user/{username}/result/DataTable_{filename}.csv"
    script:
        "script/dataTable.py"

rule Plot:
    input:
        "workflow/data/DataTable_{username}_{filename}.csv"
    output:
        "workflow/data/Plot_{username}_{filename}.csv",
        "user/{username}/result/Plot_{filename}.csv"
    script:
        "script/Plot.py"