# rabbitmq.conf
# Consumer timeout — GRN inference 작업은 데이터셋에 따라 수일~수주 소요 가능
# 30일(2592000000ms). RabbitMQ 4.x는 0/false 미지원이므로 최대값으로 설정.
# task_acks_late=False이므로 실질적 영향 없으나 방어적 설정.
consumer_timeout = 2592000000
queue_master_locator=min-masters
queue_leader_locator=balanced
